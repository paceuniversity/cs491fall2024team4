import wmLight from '../../assets/wordmark-light.svg';

const WordmarkLight = ({ size = ''}) => (
    <img src={wmLight} alt='Sky blue and royal blue SetterSocial wordmark' style={{ width: size, height: 'auto'}} />
);

export default WordmarkLight;